# ========================================================================
# Universal Session Viewer - Environment Configuration
# ========================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env to version control!
# The .env file is excluded via .gitignore
# ========================================================================

# ========================================================================
# macOS Code Signing & Notarization
# ========================================================================
# Required for signed/notarized macOS builds
# Skip these for local development (use npm run build:mac:unsigned)

# Your Apple Developer account email
# https://developer.apple.com/account
APPLE_ID=

# App-specific password for notarization
# Generate at: https://appleid.apple.com/account/manage
# Go to: Sign-In and Security > App-Specific Passwords > Generate
# Format: xxxx-xxxx-xxxx-xxxx
APPLE_APP_SPECIFIC_PASSWORD=

# Your 10-character Apple Team ID
# Find at: https://developer.apple.com/account/#/membership
# Example: ABCD1234EF
APPLE_TEAM_ID=

# ========================================================================
# Build Options
# ========================================================================

# Skip notarization for local development builds
# Set to 'true' to build without Apple credentials
# Default: false
SKIP_NOTARIZATION=false

# ========================================================================
# Windows Code Signing (Optional)
# ========================================================================
# Only needed for signed Windows builds
# Skip these for unsigned builds

# Path to your code signing certificate (.pfx)
# WIN_CSC_LINK=

# Password for the certificate
# WIN_CSC_KEY_PASSWORD=

# ========================================================================
# GitHub Publishing (CI/CD)
# ========================================================================
# Only needed for automated releases
# In GitHub Actions, use ${{ secrets.GITHUB_TOKEN }} instead

# GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, write:packages
# GH_TOKEN=

# ========================================================================
# Development
# ========================================================================

# Enable verbose logging
# NODE_ENV=development

# Debug specific modules
# DEBUG=electron*,universal-session-viewer*
